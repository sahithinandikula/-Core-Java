//create a java application where we havw one class it contains one static inner class which contains one static and non static method and a member inner class which contains non static method inside the class we have another member class which contains a non static method.In this method we have a local inner class which contains a non static method and a parameterized constructor then invoke all the possible methods under main of separate class Test. 